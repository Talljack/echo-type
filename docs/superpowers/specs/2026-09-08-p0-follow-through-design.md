# P0 follow-through: one plan and guided pronunciation

User rejected the duplicate custom-plan compatibility panel and the compressed goal picker screenshot, and authorized continued P0 implementation without repeated approval questions.

## Dashboard decision

Keep one Today workspace. Remove the separate legacy plan panel and the old horizontal goal banner. Inline Learning settings within Today preserves goal selection, daily session target and access to assessment. Goal description occupies its own full-width row; choices wrap in a responsive grid rather than reserving a non-shrinking horizontal strip. Daily completed sessions are compared with the saved target; session evidence, not clicking, drives progress. Existing saved goals/tasks are not deleted. Do not advertise legacy task generation that no longer drives the default course path.

Visual direction: quiet indigo learning utility, existing Poppins/Open Sans, slate text and white surface; 12px buttons and 24px surfaces. Tailwind only. Compact status and direct actions instead of another marketing hero. Verify 375/768/1024/1440 widths and increased font size.

## Pronunciation decision

Continue the existing P0 studio rather than create another score system. Add evidence-derived per-sound state, a deterministic next-practice recommendation, and next-sound navigation. Only recorded (or professional) evidence qualifies a sound as practiced for continuation; legacy and browser text matching never imply mastery. Keep 48-entry chart, audio comparison and existing professional path. Surface recent evidence on the selected sound and clearly label what remains untested. Link chart choice and relevant minimal pair so listening and recording do not contradict each other. A correct later listening attempt can resolve the matching listening Weak Spot; do not resolve acoustic weaknesses using browser recognition or listening success.

Preserve data and credentials, account-scoped storage, cancellation and permissions. No paid calls, push, deploy or new cloud schema. Browser verification uses isolated data and mocked device audio and must state that limitation.

# Activity card sizing

Approved in conversation: keep desktop statistics alongside one another, enlarge the mini heatmap modestly in single-column layouts, and add period, active-day and date context. Do not stretch cells across a full-width dashboard or change stored activity.

Use an auto-fit grid with cards capped at 26rem. Heatmap width is fluid up to 20rem; square cells share available width, with 4px gutters and seven aligned rows. Show the actual trailing period, active days and first/last dates. Preserve bilingual copy, native card styling, empty state and Details navigation.

Verify summary counts, date alignment, empty state and rendering at 375px and desktop widths. No migrations or native-shell changes.
